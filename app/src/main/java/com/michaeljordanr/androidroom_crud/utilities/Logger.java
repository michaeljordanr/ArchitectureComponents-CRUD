package com.michaeljordanr.androidroom_crud.utilities;

import android.arch.lifecycle.LiveData;
import android.util.Log;

import java.util.List;

import com.michaeljordanr.androidroom_crud.person.db.entity.PersonEntity;
import com.michaeljordanr.androidroom_crud.person.model.Person;

/**
 * Created by husaynhakeem on 6/12/17.
 */

public class Logger {
    
    
    private static final String TAG = Logger.class.getName();
    
    
//    public static void displayAdressesInLog(Address[] addresses) {
//
//        if (addresses == null)
//            return;
//
//        for (Address address: addresses) {
//            Log.d(TAG, "Address street: " + address.state + ", address city: " + address.city + ", address state: " + address.state + ", adress postal code: " + address.postCode);
//        }
//    }


//    public static void displayPersonsInLog(LiveData<List<PersonEntity>> persons) {
//
//        if (persons == null)
//            return;
//
//        for (Person person: persons) {
//            Log.d(TAG, "Person id: " + person.getId() + ", person name: " + person.getFirstName() + " " + person.getLastName());
//        }
//    }


//    public static void displayCatsInLog(CatEntity[] cats) {
//
//        if (cats == null)
//            return;
//
//        for (CatEntity cat: cats) {
//            Log.d(TAG, "CatEntity id: " + cat.id + ", cat name: " + cat.name + ", cat age: " + cat.age + ", cat owner: " + cat.hoomanId);
//        }
//    }
}
